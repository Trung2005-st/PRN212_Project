<Window x:Class="QuitHubWPF_App.UserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="UserWindow" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen" ResizeMode="CanResize">

    <Window.Resources>
        <LinearGradientBrush x:Key="GreenGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#66BB6A" Offset="0"/>
            <GradientStop Color="#388E3C" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="UserCardStyle" TargetType="Button">
            <Setter Property="Margin" Value="20"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2E7D32"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#388E3C"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#C8E6C9"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="2" Opacity="0.2"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="pack://application:,,,/Assets/Images/backgroundImg1.png" Stretch="Fill"/>
        </Grid.Background>

        <DockPanel>
            <!-- Navbar -->
            <DockPanel DockPanel.Dock="Top" Background="{StaticResource GreenGradient}" Height="50" LastChildFill="False">
                <TextBlock x:Name="txtWelcome"
                           Foreground="White"
                           FontWeight="Bold"
                           FontSize="16"
                           VerticalAlignment="Center"
                           Margin="20,0,0,0" />

                <Button Content="Logout"
                        Click="Logout_Click"
                        Background="Red"
                        Foreground="White"
                        FontWeight="Bold"
                        Width="80"
                        Height="30"
                        Margin="0,0,20,0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        BorderThickness="0"
                        DockPanel.Dock="Right"/>
            </DockPanel>

            <!-- Action cards -->
            <WrapPanel Margin="40" HorizontalAlignment="Center" VerticalAlignment="Top">

                <Button Style="{StaticResource UserCardStyle}" Width="200" Height="120" Click="ManageInfo_Click">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="" FontSize="26" Margin="0,0,0,8" HorizontalAlignment="Center"/>
                        <TextBlock Text="Manage Info" TextAlignment="Center" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource UserCardStyle}" Width="200" Height="120" Click="CreatePlan_Click">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="" FontSize="26" Margin="0,0,0,8" HorizontalAlignment="Center"/>
                        <TextBlock Text="Create Quit Plan" TextAlignment="Center" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource UserCardStyle}" Width="200" Height="120" Click="ViewPlans_Click">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="" FontSize="26" Margin="0,0,0,8" HorizontalAlignment="Center"/>
                        <TextBlock Text="View Quit Plans" TextAlignment="Center" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource UserCardStyle}" Width="200" Height="120" Click="Feedback_Click">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets"  Text="" FontSize="26" Margin="0,0,0,8" HorizontalAlignment="Center"/>
                        <TextBlock Text="Feedback &amp; Rating" TextAlignment="Center" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource UserCardStyle}" Width="200" Height="120" Click="Counseling_Click">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="" FontSize="26" Margin="0,0,0,8" HorizontalAlignment="Center"/>
                        <TextBlock Text="Counseling Schedule" TextAlignment="Center" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

            </WrapPanel>
        </DockPanel>
    </Grid>
</Window>
